<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img" aria-label="NextPaper">
  <style>
    :root {
      --accent: #006cac;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --accent: #ff6b01;
      }
    }

    rect {
      fill: transparent;
    }

    text {
      fill: var(--accent);
      font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      font-size: 42px;
      font-weight: 700;
      letter-spacing: -0.08em;
    }
  </style>
  <rect width="64" height="64" rx="14" />
  <text x="50%" y="50%" dominant-baseline="central" text-anchor="middle">N</text>
</svg>
